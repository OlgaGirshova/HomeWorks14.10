/*
// Задача 10. Напишите программу, которая принимает на вход трехзначное число, а на выходе показывает вторую цифру этого числа.
Console.WriteLine("Enter a three-digit number ");
int number = Convert.ToInt32(Console.ReadLine());
int result = number / 10 % 10;
Console.WriteLine($"The result is: {result} ");
*/
/*
//Задача 13. Напишите программу, которая выводит третьюцифру заданного числа или сообщает, что третьей цифры нет.
Console.Write("Enter a number: ");
int number = Convert.ToInt32(Console.ReadLine());
string numberText = Convert.ToString(number);
if (numberText.Length > 2)
{
  Console.WriteLine("The third number is: " + numberText[2]);
}
else
{
  Console.WriteLine("No third number");
}
*/
/*
//Задача 15.Напишите программу, которая принимает на вход цифру, обозначающую день недели, и проверяет, является ли этот день выходным
Console.WriteLine("Enter a day of the week from 1 to 7: ");
int number = Convert.ToInt32(Console.ReadLine());

bool Weekend (int number)
{
    if (number == 6 | number == 7)
    {
        return true;
    }
    else
    {
        return false;
    }
}

if (number < 1 | number > 7) Console.WriteLine("Wrong number, it should be from 1 to 7 ");
else
{
    if (Weekend (number)) Console.WriteLine("Weekend");
    else
    {
        Console.WriteLine("Working day");
    }
}
*/
